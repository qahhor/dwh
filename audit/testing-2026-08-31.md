# Testing audit — 2026-08-31

## Инвентаризация

- Backend: 39 test files, 160 методов с `@Test`.
- Frontend: 25 spec files, 58 `it/test` cases; фактический web-instance run — 53 cases.
- Browser E2E: 5 spec files, 8 cases.
- Coverage threshold/report (JaCoCo, Istanbul threshold, Codecov) не найден. `AuditCoverageTest` — доменный тест, не инструмент покрытия.

| Наблюдение | Риск | Доказательство | Минимальная рекомендация / критерий | Усилие | Приоритет |
|---|---|---|---|---|---|
| Архитектурный backend gate падает | CI/RC не green, новое нарушение слоя уже в коде | `ModularArchitectureTest.java:39-80`; evidence report | Исправить dependency; выполнить полный `mvn -B verify`; 0 failures | S | P0 |
| web-instance: 12 failed / 41 passed | Доступность и login regressions подтверждены тестами | `app-shell.component.spec.ts`, `login.component.spec.ts`, tasks/projects/users/custom-fields specs; evidence | Синхронизировать mocks с SSO/i18n и вернуть role/aria-label; 53/53 pass | M | P0 |
| CI собирает Angular apps, но не запускает unit tests | Красные unit-тесты могут попасть в main | `.github/workflows/ci.yml:56-66` содержит только `npm ci` и `npm run build` | Добавить `npm test -- --run` для обоих apps перед build | S | P0 |
| 8 E2E недостаточно для заявленных критических flows | AuthZ, файлы, аудит, recovery и новые SSO/modules/analytics не защищены от регрессий | `e2e/tests/*.spec.ts`; `e2e/playwright.config.ts:8-31` | Не гнаться за числом: добавить 6 release-critical сценариев с server assertions: login/session, RBAC/IDOR, task CRUD+idempotency, upload/download, audit, CP heartbeat/module rejection | M | P1 |
| Нет security regression tests для найденных attack paths | Исправления могут откатиться незаметно | Поиск tests по OAuth/SSO, custom module callback и javascript markdown ссылкам — не найдено | Каждый P0 security fix сопровождается negative test до закрытия карточки | M | P0 |
| Coverage gate отсутствует | Нельзя измерить, что критический код действительно исполняется | В POM/package scripts нет JaCoCo/coverage threshold | Ввести JaCoCo отчёт и diff-coverage для изменяемых critical packages; первый релизный критерий — 100% critical-flow tests, не искусственный общий процент | M | P1 |
| E2E выполняется последовательно, на clean compose | Это снижает flaky/race risk и изолирует данные | `e2e/playwright.config.ts:8-16`; `.github/workflows/ci.yml:99-141` | Сохранить clean environment; публиковать trace/screenshot/video только при failure | S | P2 |
| Нет зафиксированного restore smoke test | Backup может оказаться невосстановимым | `scripts/prod/restore.sh`, `restore.ps1`; drill artifact не найден | CI/weekly: backup ephemeral DB → destroy → restore → schema/data assertions | M | P1 |
| web-cp не подтверждён локально из-за Node mismatch | Локальная проверка неполна | `apps/web-cp/package.json:13`; evidence | Добавить `.nvmrc`/`.node-version` и validate toolchain; CI Node 22 остаётся canonical | S | P1 |

## Матрица критических потоков

| Поток | Unit/integration | E2E | Статус |
|---|---|---|---|
| Password login/session/CSRF | Есть частично | invalid login + protected route | Частично |
| RBAC/data scope/IDOR | Есть permission tests | Только navigation admin | Недостаточно |
| Task/project CRUD | Есть частично | Один task flow | Частично |
| File upload/download/quota | Есть backend tests | Нет | Недостаточно |
| Audit immutability/export | Есть backend coverage | Нет | Частично |
| CP fleet/heartbeat | Есть tests + 1 E2E registration | Есть частично | Частично |
| SSO/custom modules/analytics | Тесты не найдены | Нет | Блокер для включения функций |
| Backup/restore/rollback | Нет automated drill | Нет | Недостаточно |
