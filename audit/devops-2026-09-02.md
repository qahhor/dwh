# DevOps audit — 2026-09-02

## Findings

| Observation | Risk | Evidence | Minimal recommendation | Effort | Priority |
|---|---|---|---|---|---|
| CI includes backend, frontend, release-config, clean-deploy E2E and security; actions are SHA-pinned. | Strong PR gate, but a feature-branch push alone has no remote run. | `.github/workflows/ci.yml:5-9,14-188`; public API returned zero runs for the branch. | Open a PR or add an explicitly approved `workflow_dispatch`; require all jobs and DCO before merge. | S | P0 release |
| Stable SemVer release builds five amd64/arm64 images with provenance, SBOM and keyless signatures. | Not exercised for current SHA; artifacts do not yet exist. | `.github/workflows/release.yml:5,143-172`; `README.md:115-124`. | After merge, tag one reviewed SemVer; verify digests/signatures/checksums from the release manifest. | S | P0 release |
| Production Compose is hardened and its V018→V019 path passed locally. | Local Docker Desktop is not the target Hetzner/Cloudflare environment. | `deploy/compose/docker-compose.prod.yml`; evidence bundle. | Provision target, external TLS/WAF/port checks, secret permissions, rollback and soak evidence. | M | P0 installation |
| Encrypted scheduled DB backup is present; default RPO is up to 24h and object bytes are separate. | Database-only recovery can lose uploaded files; RPO/RTO/SLA unapproved. | `architecture-overview.md:57-58,88-100`; launch checklist `:35-47`. | Test isolated DB restore plus selected local/R2 object recovery; document measured RPO/RTO and off-host age identity custody. | M | P0 installation |
| No Terraform/Kubernetes/IaC, alert rules, dashboards, log aggregation, DORA metrics or DR plan for a real target were found. | Environment drift and silent failures; repeatability cannot be audited. | Repository file search returned no Terraform/k8s/Prometheus/Grafana bundle; `architecture-overview.md:101-104`. | For the agreed single-host scope add only minimal Hetzner host/firewall/DNS inventory and alert-as-code; do not introduce Kubernetes. | M | P1 |
| Runtime images and dependency manifests scan with 0 unwaived HIGH/CRITICAL locally. | Scanner availability/rate limits can make CI nondeterministic. | Evidence bundle; CI now primes Maven cache at `ci.yml:164-177`. | Retain cache step, pin scanner/database behavior, and preserve reports as release artifacts. | S | P1 |

Cost finding: no resource/cost model was found. At the stated load, one measured
Hetzner host plus Cloudflare R2/edge is the minimum-cost baseline; select size
only after the load test and include backup/restore workspace in disk capacity.
