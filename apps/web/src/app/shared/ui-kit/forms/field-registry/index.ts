/* Vendored from @greenwhite/ui-kit (MIT) at commit 6472beb, path forms/field-registry/index.ts.
 * Per ADR-0015 this copy is ours to change; the commit above is only the
 * base for comparing later work in the kit. See NOTICE. */
export { UI_KIT_FIELD_REGISTRY, type UiKitFieldHandle, type UiKitFieldRegistry } from './field-registry.token';
export {
  SmtFormFieldRegistryBridgeDirective,
  SMT_FORM_FIELD_REGISTRY_HOST_DIRECTIVES,
} from './field-registry-bridge.directive';
