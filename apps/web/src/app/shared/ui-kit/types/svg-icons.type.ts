/* Adapter, not vendored code.
 *
 * Upstream this is a union of every sprite name the kit ships. Icons here
 * are Material Symbols, whose names are open-ended, so the type is the name
 * itself and the icon adapter maps or passes it through. */
export type SMTIcons = string;
